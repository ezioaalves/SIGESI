"""Arquivos app configuration."""

from django.apps import AppConfig


class ArquivosConfig(AppConfig):
    """Configuration for the Arquivos app."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.arquivos"
    verbose_name = "Arquivos"
