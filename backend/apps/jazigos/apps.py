"""Jazigos app configuration."""

from django.apps import AppConfig


class JazigosConfig(AppConfig):
    """Configuration for the Jazigos app."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.jazigos"
    verbose_name = "Jazigos"
