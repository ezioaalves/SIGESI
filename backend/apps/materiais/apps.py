"""Materiais app configuration."""

from django.apps import AppConfig


class MateriaisConfig(AppConfig):
    """Configuration for the Materiais app."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.materiais"
    verbose_name = "Materiais"
